# Konfigurations-Checkliste

Die einzige vollständige Übergabeliste liegt unter:

`/media/d4sd1ng/AI-Data/Projects/Jude/ZUGANGSDATEN_EINTRAGEN.md`

Geheimnisse ausschließlich in der gitignorierten Datei `AGENT/.env` eintragen. Nach jeder eingerichteten Integration ist ein echter Verbindungstest erforderlich; ein ausgefülltes Feld allein gilt nicht als Live-Abnahme.